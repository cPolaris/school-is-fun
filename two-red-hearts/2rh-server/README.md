Run server:

`node server.js`

- All business logics (i.e. `server.js`) access the database via stored procedures, no raw SQL queries
- Server is API-only

Python dependencies:

- numpy
- scipy
- mysqlclient